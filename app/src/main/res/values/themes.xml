<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Android_github_clone" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_black</item>
        <item name="colorPrimaryVariant">@color/light_gray</item>
        <item name="colorOnPrimary">@color/bgColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:navigationBarColor">@color/bgColor</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Splash Activity Theme -->
    <style name="SplashTheme" parent="Theme.Android_github_clone">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowBackground">@color/bgColor</item>
        <item name="android:statusBarColor">@color/bgColor</item>
        <item name="android:navigationBarColor">@color/bgColor</item>
    </style>

    <!--LogIn Activity-->
    <style name="LogInTheme" parent="Theme.Android_github_clone">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowBackground">@color/bgColor</item>
        <item name="android:statusBarColor">@color/bgColor</item>
        <item name="android:navigationBarColor">@color/bgColor</item>
    </style>


    <style name="circle">
        <item name="cornerSize">50%</item>
    </style>


</resources>